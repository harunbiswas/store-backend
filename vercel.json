{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.js" }],
  "builds": [
    {
      "src": "./server.js",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm run vercel-build"
      }
    }
  ]
}
